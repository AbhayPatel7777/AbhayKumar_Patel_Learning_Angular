import { DepartmentBudgetPipe } from './department-budget.pipe';

describe('DepartmentBudgetPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentBudgetPipe();
    expect(pipe).toBeTruthy();
  });
});
